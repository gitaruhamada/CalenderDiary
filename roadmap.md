# 開発ロードマップ

- 作成日: 2026-09-20
- 前提: `requirements.md` の内容が確定済みであること

本ドキュメントは `requirements.md` で定義した「カレンダー付き日記アプリ」を、どの順序で実装していくかを示すロードマップである。

## 全体方針

- データ層（IndexedDB）を先に固めてから、カレンダー・CRUD・画像・検索の順に機能を積み上げる。
  - 理由: UIを先に作ると後からIndexedDBのスキーマ変更が発生しやすく、手戻りが大きくなるため。
- カレンダー画面を日記CRUDより先に作る。
  - 理由: 日記データが無いと、カレンダー上の表示確認ができないため。

## フェーズ0: 環境構築 ✅完了

- [x] Gitリポジトリの初期化
- [x] Vite + React でプロジェクト雛形を作成
- [x] ディレクトリ構成の決定（`src/db/` `src/components/` `src/hooks/`、詳細は`CLAUDE.md`を参照）

## フェーズ1: データ層（IndexedDB） ✅完了

- [x] スキーマ定義（`id / date / title / body / images / tags / createdAt / updatedAt`）
- [x] CRUD用ラッパー関数の実装（追加・取得・更新・削除、日付範囲での取得）
- [x] 画像をBlobとして保存し、エントリから参照する仕組みの実装
- [x] UIなしでの動作確認（vitest + fake-indexeddbによる自動テスト、9件すべてパス）

## フェーズ2: カレンダー画面（コア機能）

- [ ] 月表示カレンダーの実装
- [ ] 前月／翌月への切り替え、「今日」に戻るボタン
- [ ] 日記が存在する日付へのマーク表示（複数件の場合は件数表示）
- [ ] 日付クリックでその日のエントリ一覧を表示

## フェーズ3: 日記のCRUD UI

- [ ] 新規作成フォーム（タイトル任意・本文必須）
- [ ] 閲覧・編集画面
- [ ] 削除機能（確認ダイアログ付き）
- [ ] 同一日付に複数エントリを作成・一覧表示できることの確認

## フェーズ4: 画像添付

- [ ] 画像アップロード（1エントリあたり最大5枚）
- [ ] サムネイル表示、拡大表示
- [ ] 画像の個別削除

## フェーズ5: タグ・検索・月間一覧

- [ ] タグ付与機能
- [ ] タグ一覧からの絞り込み表示
- [ ] キーワード検索（タイトル・本文）
- [ ] タグ・日付範囲による絞り込み検索
- [ ] 月ごとのタイトル・タグ一覧ビュー

## フェーズ6: データ管理・仕上げ

- [ ] JSONエクスポート／インポート機能（バックアップ用）
- [ ] レスポンシブ対応の調整（375px程度〜PC幅）
- [ ] キーボード操作・コントラストなどアクセシビリティの確認
- [ ] 実機ブラウザでの通しの動作確認

## 参照

- `requirements.md` — 機能スコープ、データモデル、非機能要件などの詳細
