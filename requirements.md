# カレンダー付き日記アプリ 要件定義書

- 作成日: 2026-09-20
- ステータス: ドラフト（レビュー待ち）

## 1. 概要

カレンダーを軸に日々の日記を記録・閲覧できる、ブラウザで動作するWebアプリケーションを開発する。
サーバーやアカウント登録は用いず、ブラウザ内にデータを保存するローカル完結型のアプリとする。

## 2. 目的・背景

- カレンダーを見ながら「この日に何をしたか」を直感的に記録・振り返りできるようにしたい。
- インストール不要で、ブラウザさえあればすぐ使える手軽さを重視する。

## 3. 想定ユーザー

- 個人利用（自分専用の日記）。マルチユーザー機能（ログイン・アカウント分離）は対象外。

## 4. 対象プラットフォーム・動作環境

- Webアプリ（PC・スマートフォンのブラウザで動作するレスポンシブデザイン）
- モダンブラウザ（Chrome / Edge / Safari の最新版）を想定
- インストール不要、サーバー通信不要（完全ローカル動作）

## 5. スコープ

### 5.1 対象とする機能（標準スコープ）

1. **カレンダー表示**
   - 月単位のカレンダー表示
   - 前月・翌月への移動、「今日」に戻るボタン
   - 日記が存在する日付には目印（ドット等）を表示し、複数件ある場合は件数がわかるようにする
   - 日付をクリックするとその日の日記一覧を表示し、選択して閲覧・編集
2. **日記のCRUD**
   - 作成：同じ日付に複数件の日記を作成可能（タイトル任意＋本文必須）
   - 閲覧：カレンダーから選択した日付の日記一覧を表示し、エントリを選んで詳細を閲覧
   - 編集：既存の日記内容を更新
   - 削除：日記の削除（削除前に確認ダイアログ）
3. **画像添付**
   - 日記に複数枚の画像を添付可能（1エントリあたり最大5枚）
   - サムネイル表示、拡大表示、個別削除
4. **タグ付け**
   - 日記に自由なタグ（複数）を付与
   - タグ一覧から絞り込み表示
5. **検索**
   - キーワード検索（タイトル・本文を対象）
   - タグ・日付範囲による絞り込み
   - 検索結果一覧から該当日記へ遷移
6. **月間一覧**
   - 月ごとに日記の件数・タイトル一覧を確認できるビュー

### 5.2 対象外（本バージョンでは実装しない）

- サーバー同期・複数端末間でのデータ共有
- ユーザー認証・複数アカウント対応
- 気分・天気などの定量記録、統計グラフ表示
- パスワードロック・暗号化
- オフライン後の自動同期（PWA化）

> 上記「対象外」は次期拡張候補として第10章に記載する。

## 6. 非機能要件

| 項目 | 内容 |
|---|---|
| データ保存 | ブラウザのIndexedDBに保存する（画像バイナリを扱うためlocalStorageではなくIndexedDBを採用）|
| データ永続性 | 同一ブラウザ・同一端末であればページを閉じても再訪時にデータが残る |
| バックアップ | 日記データをJSON形式でエクスポート／インポートできる機能を用意し、ブラウザデータ消失時の保険とする |
| パフォーマンス | 数年分（数百〜数千件）の日記を保持してもカレンダー表示・検索がストレスなく動作すること |
| レスポンシブ | スマートフォン幅（375px程度）からPC幅まで崩れず表示されること |
| アクセシビリティ | キーボード操作でのカレンダー移動、十分なコントラスト比を確保する |

## 7. 画面構成（想定）

1. **カレンダー画面（メイン画面）**
   - 月間カレンダー、日記ありマーク、月切り替え、検索・月間一覧への導線
2. **日記詳細／編集画面**
   - 選択日付の日記表示、編集モード切り替え、画像添付、タグ入力、削除ボタン
3. **検索画面**
   - キーワード／タグ／日付範囲の入力欄、結果一覧
4. **月間一覧画面**
   - 選択月の日記タイトル・タグ一覧
5. **設定／データ管理画面**
   - エクスポート／インポート機能

## 8. データ構造（案）

日記エントリ1件が持つ主な項目：

| 項目 | 型 | 必須 | 説明 |
|---|---|---|---|
| id | string | ○ | 一意なID |
| date | string (YYYY-MM-DD) | ○ | 対象日付（同一日付に複数件登録可。同日内の並び順はcreatedAt順） |
| title | string | - | タイトル（任意） |
| body | string | ○ | 本文 |
| images | 画像データの配列 | - | 添付画像（IndexedDBに直接保存） |
| tags | string[] | - | タグ一覧 |
| createdAt | datetime | ○ | 作成日時 |
| updatedAt | datetime | ○ | 更新日時 |

## 9. 技術スタック

- フロントエンド: React（Vite等のビルドツールを使用予定、詳細は実装時に決定）
- データ保存: ブラウザ標準の IndexedDB
- 画像: IndexedDBにBlobとして保存（Base64化によるデータ肥大化を避ける）
- サーバー・バックエンドは持たない（フロントエンドのみで完結）

## 10. 将来の拡張候補（本バージョン対象外）

- 気分・天気の記録と月間統計・グラフ表示
- 複数端末間でのデータ同期（クラウド連携）
- 日記のパスワード保護／暗号化
- PWA化によるオフライン対応・ホーム画面追加

## 11. 制約・前提

- ブラウザのプライベートモードやストレージクリアによりデータが失われる可能性があるため、エクスポート機能の利用を利用者に促す設計とする。
- 対象は個人利用のみで、他者との共有機能は持たない。

## 12. 未確定事項（要確認）

- [x] 1日に複数の日記エントリを許可するか、1日1件固定とするか → **複数件許可**
- [x] タイトルを必須にするか任意のままとするか → **任意のまま**
- [x] フレームワーク選定（素のJS or React等） → **React**
- [x] 画像添付枚数の上限 → **1エントリあたり最大5枚**
